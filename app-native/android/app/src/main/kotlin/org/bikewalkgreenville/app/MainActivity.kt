package org.bikewalkgreenville.app

import io.flutter.embedding.android.FlutterActivity

// The reroute ToneGenerator channel lived here until v1.11.0: even the soft
// TONE_PROP_ACK read as an alarm on the road, so reroutes are voice-only now.
class MainActivity : FlutterActivity()
