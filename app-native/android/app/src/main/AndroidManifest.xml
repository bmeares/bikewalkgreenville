<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Android 13+: the ongoing turn-by-turn notification. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- No background location: the app only ever asks for fixes while it is
         on screen, so geolocator never gets a foregroundNotificationConfig and
         its service never calls startForeground(). Dropping these two keeps
         Play review from demanding a FOREGROUND_SERVICE_LOCATION
         justification we would have nothing to write in.
         The <service> itself MUST stay — see below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:node="remove"/>

    <application
        android:label="Bike Walk Greenville"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- DO NOT REMOVE THIS SERVICE. It looks like dead weight for an app
             that never goes to the background, but geolocator's position
             STREAM is served through it: the plugin binds it at startup, and
             StreamHandlerImpl.onListen bails out with a bare `return` — no
             error, no endOfStream — when the bind never happened. Removing it
             does not crash and does not log anything Dart can see; it just
             makes getPositionStream emit nothing, forever, so every
             turn-by-turn trip freezes the moment it starts.
             getCurrentPosition() uses a different handler and keeps working,
             which is what disguised this as "the follow camera is stuck".
             Stripping foregroundServiceType keeps it a plain bound service:
             startForeground() is only ever reached from enableBackgroundMode(),
             which needs a foregroundNotificationConfig we never pass. -->
        <service
            android:name="com.baseflow.geolocator.GeolocatorLocationService"
            tools:remove="android:foregroundServiceType"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package visibility (Android 11+): without these, launchUrl cannot
             resolve the BCycle app and every deep link falls through to the
             browser / Play Store. -->
        <package android:name="com.bcycle"/>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="bcycle"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
